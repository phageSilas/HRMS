# HRMS 后端系统开发前置说明
> **文档版本**: v1.1  
**生效日期**: 2026-07-11  
**文档性质**: 所有后端开发的最顶级约束，独立不依赖其他任何文档
>

---

## 一、项目架构总览
### 1.1 四个顶层模块
HRMS 采用**单体分层架构**，通过 Maven 多模块组织代码，所有模块打包为一个 JAR 部署。

| 模块 | 职责 | 禁止事项 |
| --- | --- | --- |
| `hrms-common` | 公共基础能力（返回体、异常、枚举、工具类、基础实体、安全上下文）**只做能力，不做业务** | 禁止反向依赖业务模块、禁止包含 Controller、禁止涉及数据表 |
| `hrms-system` | 系统底座（权限体系、组织架构、附件管理、日志管理） | 禁止依赖 business |
| `hrms-business` | 核心业务（员工、入转调离、考勤、薪资、审批、个人中心、AI智能助手） | - |
| `hrms-server` | 启动装配，不承载业务逻辑 | 禁止写 Controller/Service/Mapper |


### 1.2 模块依赖关系
```plain
hrms-server → hrms-business → hrms-system → hrms-common
```

### 1.3 业务模块划分
#### hrms-system（系统管理域，4 个子模块）
| 子模块 | 包路径 | 职责 | 数据表 |
| --- | --- | --- | --- |
| auth | com.hrms.system.auth | 用户、角色、菜单、登录认证 | sys_user、sys_role、sys_menu、sys_user_role、sys_role_menu |
| organization | com.hrms.system.organization | 部门、职位、字典 | sys_dept、sys_post、sys_dict_type、sys_dict_data |
| file | com.hrms.system.file | 附件管理 | sys_file |
| log | com.hrms.system.log | 操作日志、登录日志 | sys_operate_log、sys_login_log |


#### hrms-business（业务域，7 个子模块）
| 子模块 | 包路径 | 职责 |
| --- | --- | --- |
| employee | com.hrms.business.employee | 员工档案管理 |
| personnel | com.hrms.business.personnel | 入转调离流程 |
| attendance | com.hrms.business.attendance | 考勤管理 |
| salary | com.hrms.business.salary | 薪资管理 |
| approval | com.hrms.business.approval | 审批中心 |
| mycenter | com.hrms.business.mycenter | 个人中心 |
| ai | com.hrms.business.ai | AI智能助手 |


### 1.4 开发协作分工
| 开发者 | 负责模块 | 包路径 |
| --- | --- | --- |
| 地基搭建者 | hrms-common + file + log | com.hrms.common, com.hrms.system.file, com.hrms.system.log |
| 成员 A | auth + organization | com.hrms.system.auth, com.hrms.system.organization |
| 成员 B | employee + personnel | com.hrms.business.employee, com.hrms.business.personnel |
| 成员 C | attendance + salary | com.hrms.business.attendance, com.hrms.business.salary |
| 成员 D | approval + mycenter + ai | com.hrms.business.approval, com.hrms.business.mycenter, com.hrms.business.ai |


---

## 二、开发规范
### 2.1 命名规范
| 类型 | 命名格式 | 示例 |
| --- | --- | --- |
| 实体类 | `XxxEntity` | UserEntity, EmployeeEntity |
| DTO | `XxxRequestDTO`, `XxxQueryDTO`, `XxxCommandDTO` | UserCreateRequestDTO, EmployeeQueryDTO |
| VO | `XxxVO`, `XxxPageVO` | UserVO, EmployeePageVO |
| Mapper | `XxxMapper` | UserMapper, EmployeeMapper |
| Service | `XxxService` | UserService, EmployeeService |
| Service 实现 | `XxxServiceImpl` | UserServiceImpl, EmployeeServiceImpl |
| Controller | `XxxController` | UserController, EmployeeController |
| 转换器 | `XxxConvert` | UserConvert, EmployeeConvert |
| 枚举 | `XxxEnum` | GenderEnum, EmployeeStatusEnum |


### 2.2 分层规范
后端业务实现统一按照以下分层组织：

```plain
com.hrms.{module}
├── controller/     # 接口控制层
├── service/        # 业务服务接口
│   └── impl/       # 业务服务实现
├── mapper/         # 数据访问层
├── entity/         # 数据库实体
├── dto/            # 请求或过程数据传输对象
├── vo/             # 返回视图对象
├── convert/        # 对象转换器
├── enums/          # 模块内业务枚举
├── config/         # 模块配置
└── exception/      # 模块异常
```

### 2.3 接口规范
#### HTTP 语义规范
| 方法 | 用途 |
| --- | --- |
| `GET` | 查询 |
| `POST` | 新增、流程发起、复杂条件查询 |
| `PUT` | 全量更新 |
| `PATCH` | 部分更新、状态变更 |
| `DELETE` | 逻辑删除 |


#### 分页接口规范
分页查询至少包含：

+ `pageNum`：页码（从 1 开始）
+ `pageSize`：每页大小
+ `total`：总记录数
+ `records`：数据列表

#### 统一返回体
所有接口使用 `Result<T>` 统一返回：

```java
// 成功
Result.success(data)
Result.success()

// 失败
Result.failure(ErrorCode.XXX)
Result.failure(code, message)
```

**响应格式**：

```json
{
  "code": 20000,
  "message": "success",
  "data": { ... },
  "timestamp": 1720684800000
}
```

### 2.4 错误码规范
#### 2.4.1 码段划分
| 码段 | 类型 | HTTP 状态码 | 说明 |
| :---: | :---: | :---: | --- |
| `20000` | 成功 | 200 | 请求正常处理 |
| `400xx` | 参数/客户端错误 | 400 | 参数校验失败、请求参数错误等 |
| `401xx` | 认证错误 | 401 | Token 无效/过期、账号锁定等 |
| `403xx` | 权限错误 | 403 | 无角色/数据/字段权限 |
| `404xx` | 资源错误 | 404 | 数据不存在、用户不存在 |
| `409xx` | 冲突错误 | 409 | 乐观锁冲突、数据重复 |
| `500xx` | 服务端错误 | 500 | 系统/数据库/远程服务异常 |


#### 2.4.2 完整异常码对照表
| 异常码 | 枚举常量 | 说明 | 触发场景 |
| :---: | --- | --- | --- |
| 20000 | SUCCESS | 成功 | 请求正常处理 |
| 40000 | BAD_REQUEST | 请求参数错误 | 通用参数错误 |
| 40001 | PARAM_VALIDATION_FAILED | 参数校验失败 | `@Valid` 校验不通过 |
| 40002 | MISSING_REQUIRED_PARAM | 缺少必填参数 | 必填字段未传 |
| 40100 | UNAUTHORIZED | 未认证 | 未携带 Token 请求需认证接口 |
| 40101 | TOKEN_EXPIRED | Token 已过期 | Token 超过有效期 |
| 40102 | TOKEN_INVALID | Token 无效 | Token 签名校验失败 |
| 40103 | ACCOUNT_LOCKED | 账号已锁定 | 登录失败超过 5 次，锁定 30 分钟 |
| 40300 | FORBIDDEN | 无角色/菜单权限 | 用户不拥有所需权限标识 |
| 40301 | DATA_SCOPE_DENIED | 数据权限范围外 | 访问非本人/非本部门数据 |
| 40302 | FIELD_PERMISSION_DENIED | 字段级无权限 | 访问无权限字段 |
| 40400 | NOT_FOUND | 资源不存在 | 数据 ID 查询无结果 |
| 40401 | USER_NOT_FOUND | 用户不存在 | 用户名查询无结果 |
| 40900 | CONFLICT | 数据冲突（乐观锁） | `version` 不匹配，数据已被修改 |
| 40901 | DATA_DUPLICATE | 数据重复 | 唯一索引冲突 |
| 50000 | SYSTEM_ERROR | 系统内部错误 | 未预期异常 |
| 50001 | REMOTE_SERVICE_ERROR | 远程服务调用失败 | RabbitMQ/外部服务异常 |
| 50002 | DATABASE_ERROR | 数据库操作异常 | SQL 执行失败、死锁 |


#### 2.4.3 模块业务错误码
各模块在码段范围内自行定义业务错误码，统一通过 `GlobalException` 抛出：

| 模块 | 码段 | 示例 |
| :--- | :---: | :--- |
| 权限体系 | 40011~40013 | 40011=用户名已存在, 40012=手机号已存在, 40013=角色ID不存在 |
| 组织架构 | 40021~40023 | 40021=部门编码已存在, 40022=上级部门不存在, 40023=部门层级超限 |
| 字典 | 40030 | 40030=字典类型编码不存在 |
| 员工档案 | 40031~40033 | 40031=手机号已被占用, 40032=所属部门不存在, 40033=职位不存在 |
| 入转调离 | 40041~40044 | 40041=申请不存在, 40042=非草稿无法提交, 40043=审批调用失败, 40044=审批未通过 |
| 考勤管理 | 40050~40053, 40060~40062 | 40050=未配置考勤组, 40060=余额不足, 40061=天数不能为0, 40062=超过30天限制 |
| 薪资管理 | 40070~40071, 40080~40081 | 40070=批次重复, 40080=验证失败, 40081=验证超限锁定 |
| 审批中心 | 40090~40091 | 40090=审批任务不存在, 40091=已被处理 |
| 个人中心 | 40110~40111 | 40110=旧密码错误, 40111=新密码与最近3次相同 |


### 2.5 枚举使用规范
| 场景 | 规范 |
| --- | --- |
| 数据库存储 | TINYINT 类型，存储编码值（整数） |
| Java 传递 | 使用枚举类，通过 `getCode()` 获取编码 |
| 前端接收 | 返回编码值（整数），前端通过字典映射显示文案 |
| 比较方式 | 禁止硬编码 `if (status == 1)`，必须用枚举比较 |


**正确示例**：

```java
// 正确：使用枚举比较
if (employee.getEmploymentStatus() == EmployeeStatusEnum.FORMAL.getCode()) {
    // ...
}

// 正确：使用枚举类
if (EmployeeStatusEnum.FORMAL.equals(EmployeeStatusEnum.getByCode(status))) {
    // ...
}
```

**错误示例**：

```java
// 错误：硬编码
if (status == 2) {
    // ...
}
```

### 2.6 异常处理规范
**全局统一拦截**：

`GlobalExceptionHandler` 在 hrms-common 中全局统一拦截，禁止各模块单独配置。

**异常定义规则**：

1. 业务异常统一抛出 `GlobalException`
2. 传入对应的 `ErrorCode`
3. 各模块可定义模块专有错误码，但统一用 `GlobalException`

**正确示例**：

```java
// 正确：统一使用 GlobalException
public void updateEmployee(EmployeeUpdateDTO dto) {
    EmployeeEntity employee = employeeMapper.selectById(dto.getId());
    if (employee == null) {
        throw new GlobalException(ErrorCode.EMPLOYEE_NOT_FOUND);
    }
    // ...
}

// 正确：模块自定义错误码
public class EmployeeErrorCode {
    public static final ErrorCode EMPLOYEE_NOT_FOUND = 
        new ErrorCode(40031, "员工不存在");
    public static final ErrorCode DUPLICATE_EMPLOYEE_NO = 
        new ErrorCode(40032, "工号重复");
}
```

### 2.7 MyBatis-Plus 自动填充规范
**实现归属**：

`MetaObjectHandler` 在 **hrms-common** 中统一实现，禁止各模块单独实现。

**填充规则**：

| 字段 | 填充时机 | 填充值来源 |
| --- | --- | --- |
| `create_by` | INSERT | SecurityContextHolder.getUserId() |
| `create_time` | INSERT | LocalDateTime.now() |
| `update_by` | INSERT/UPDATE | SecurityContextHolder.getUserId() |
| `update_time` | INSERT/UPDATE | LocalDateTime.now() |


**实现示例**：

```java
/**
 * MyBatis-Plus 自动填充处理器
 * 统一在 hrms-common 中实现
 */
@Component
public class MyMetaObjectHandler implements MetaObjectHandler {

    @Override
    public void insertFill(MetaObject metaObject) {
        this.strictInsertFill(metaObject, "createBy", Long.class, 
            SecurityContextHolder.getUserId());
        this.strictInsertFill(metaObject, "createTime", LocalDateTime.class, 
            LocalDateTime.now());
        this.strictInsertFill(metaObject, "updateBy", Long.class, 
            SecurityContextHolder.getUserId());
        this.strictInsertFill(metaObject, "updateTime", LocalDateTime.class, 
            LocalDateTime.now());
    }

    @Override
    public void updateFill(MetaObject metaObject) {
        this.strictUpdateFill(metaObject, "updateBy", Long.class, 
            SecurityContextHolder.getUserId());
        this.strictUpdateFill(metaObject, "updateTime", LocalDateTime.class, 
            LocalDateTime.now());
    }
}
```

**实体类使用**：

```java
public class EmployeeEntity extends BaseEntity {
    
    @TableField(fill = FieldFill.INSERT)
    private Long createBy;

    @TableField(fill = FieldFill.INSERT)
    private LocalDateTime createTime;

    @TableField(fill = FieldFill.INSERT_UPDATE)
    private Long updateBy;

    @TableField(fill = FieldFill.INSERT_UPDATE)
    private LocalDateTime updateTime;
}
```

> **禁止事项**：
>
> + 禁止各模块单独实现 `MetaObjectHandler`
> + 禁止在业务代码中手动 set createBy/createTime/updateBy/updateTime
>

### 2.8 认证上下文规范
**规范要求**：

登录用户信息（userId、deptId、roleIds）统一由 `com.hrms.common.security.SecurityContextHolder` 管理（基于 ThreadLocal），所有模块必须使用该工具类获取当前用户。

**禁止事项**：

+ 禁止自行解析 Token
+ 禁止在各模块重复实现用户上下文管理

**正确示例**：

```java
// 正确：使用 SecurityContextHolder 获取当前用户
public void createEmployee(EmployeeCreateDTO dto) {
    Long currentUserId = SecurityContextHolder.getUserId();
    Long currentDeptId = SecurityContextHolder.getDeptId();
    // ...
}
```

**工具类定义**：

```java
/**
 * 安全上下文持有者
 * 基于 ThreadLocal 存储当前登录用户信息
 */
public class SecurityContextHolder {

    private static final ThreadLocal<UserContext> CONTEXT = new ThreadLocal<>();

    public static Long getUserId() {
        UserContext context = CONTEXT.get();
        return context != null ? context.getUserId() : null;
    }

    public static Long getDeptId() {
        UserContext context = CONTEXT.get();
        return context != null ? context.getDeptId() : null;
    }

    public static List<Long> getRoleIds() {
        UserContext context = CONTEXT.get();
        return context != null ? context.getRoleIds() : Collections.emptyList();
    }
}
```

---

## 三、公共基础设施
### 3.1 统一返回体 Result
**路径**：`com.hrms.common.web.Result`

**字段定义**：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `code` | Integer | 状态码，20000 表示成功 |
| `message` | String | 提示信息 |
| `data` | T | 数据体 |
| `timestamp` | Long | 服务器时间戳 |


**使用示例**：

```java
// 成功返回
return Result.success(userVO);
return Result.success();

// 失败返回
return Result.failure(ErrorCode.PARAM_ERROR);
return Result.failure(ErrorCode.USER_NOT_FOUND, "用户不存在");
```

### 3.2 错误码定义
**路径**：`com.hrms.common.exception.ErrorCode`

**定义示例**：

```java
public class ErrorCode {
    private final Integer code;
    private final String message;
    
    // 成功
    public static final ErrorCode SUCCESS = new ErrorCode(20000, "操作成功");
    
    // 参数错误（400xx）
    public static final ErrorCode PARAM_ERROR = new ErrorCode(40001, "参数错误");
    
    // 认证错误（401xx）
    public static final ErrorCode UNAUTHORIZED = new ErrorCode(40100, "未登录");
    public static final ErrorCode TOKEN_EXPIRED = new ErrorCode(40101, "Token已过期");
    public static final ErrorCode TOKEN_INVALID = new ErrorCode(40102, "Token无效");
    public static final ErrorCode ACCOUNT_LOCKED = new ErrorCode(40103, "账号已锁定");
    
    // 权限错误（403xx）
    public static final ErrorCode FORBIDDEN = new ErrorCode(40300, "无权限");
    public static final ErrorCode DATA_SCOPE_DENIED = new ErrorCode(40301, "数据权限不足");
    
    // 系统错误（500xx）
    public static final ErrorCode SYSTEM_ERROR = new ErrorCode(50000, "系统内部错误");
    public static final ErrorCode DB_ERROR = new ErrorCode(50002, "数据库异常");
}
```

### 3.3 BaseEntity 公共字段
**路径**：`com.hrms.common.entity.BaseEntity`

**字段定义**：

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| `id` | Long | 主键ID |
| `createBy` | Long | 创建人（自动填充） |
| `createTime` | LocalDateTime | 创建时间（自动填充） |
| `updateBy` | Long | 更新人（自动填充） |
| `updateTime` | LocalDateTime | 更新时间（自动填充） |
| `isDeleted` | Integer | 逻辑删除标记，默认 0 |
| `version` | Integer | 乐观锁版本号，默认 0 |


**实体类示例**：

```java
@TableName("sys_user")
public class UserEntity extends BaseEntity {
    private String username;
    private String password;
    // ... 其他字段
}
```

> **重要**：所有业务表实体必须继承 `BaseEntity`，MyBatis-Plus 会自动填充公共字段。
>

### 3.4 枚举清单
**路径**：`com.hrms.common.enums`

| 枚举类 | 说明 | 编码值示例 |
| --- | --- | --- |
| `BaseEnum` | 枚举基类接口 | - |
| `EmployeeStatusEnum` | 员工状态 | 1-试用期, 2-正式, 3-待离职, 4-已离职 |
| `GenderEnum` | 性别 | 1-男, 2-女 |
| `HireTypeEnum` | 入职类型 | 1-全职, 2-兼职, 3-实习 |
| `ContractTypeEnum` | 合同类型 | 1-固定期限, 2-无固定期限, 3-劳务合同 |
| `LeaveTypeEnum` | 请假类型 | ANNUAL-年假, COMPASSIONATE-调休, SICK-病假, PERSONAL-事假, MARRIAGE-婚假, MATERNITY-产假, FUNERAL-丧假 |
| `ApprovalStatusEnum` | 审批状态 | 0-草稿, 1-审批中, 2-已通过, 3-已驳回, 4-已撤回 |
| `TaskStatusEnum` | 审批任务状态 | 0-待处理, 1-已处理, 2-已转交 |
| `ApproveResultEnum` | 审批结果 | 1-通过, 2-驳回, 3-转交 |
| `DataScopeEnum` | 数据权限范围 | 1-仅本人, 2-本部门, 3-本部门及下属部门, 4-全部 |
| `MenuTypeEnum` | 菜单类型 | 1-目录, 2-菜单, 3-按钮 |
| `SalaryBatchStatusEnum` | 薪资批次状态 | DRAFT-草稿, CALCULATING-计算中, PENDING-待确认, APPROVED-已通过 |
| `AttendanceStatusEnum` | 考勤状态 | NORMAL-正常, LATE-迟到, EARLY_LEAVE-早退, ABSENCE-旷工 |
| `BizTypeEnum` | 业务类型 | ENTRY-入职, REGULAR-转正, TRANSFER-调岗, LEAVE-离职 |


---

## 四、数据库设计约定
### 4.1 公共字段约定
所有业务表必须包含以下公共字段（继承 BaseEntity）：

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| `id` | BIGINT UNSIGNED | 主键 |
| `create_by` | BIGINT UNSIGNED | 创建人（自动填充） |
| `create_time` | DATETIME | 创建时间（自动填充） |
| `update_by` | BIGINT UNSIGNED | 更新人（自动填充） |
| `update_time` | DATETIME | 更新时间（自动填充） |
| `is_deleted` | TINYINT(1) | 逻辑删除标记，默认 0 |


### 4.2 表命名规范
| 表前缀 | 说明 | 示例 |
| --- | --- | --- |
| `sys_` | 系统表（用户、角色、菜单、部门、职位、字典、文件、日志） | sys_user, sys_role, sys_dept |
| `hr_` | 业务表（员工、入转调离、考勤、薪资、审批、AI） | hr_employee, hr_attendance_record |


### 4.3 待建表设计约束
以下待建表必须遵守额外约束：

#### 申请类表（personnel 模块）
| 表名 | 必须包含字段 |
| --- | --- |
| hr_entry_application | approval_status, approval_instance_id |
| hr_regular_application | approval_status, approval_instance_id |
| hr_transfer_application | approval_status, approval_instance_id |
| hr_leave_application | approval_status, approval_instance_id |


#### 请假申请表（attendance 模块）
| 表名 | 必须包含字段 |
| --- | --- |
| hr_leave_request | approval_status, approval_instance_id |


#### 字段说明
| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| `approval_status` | TINYINT | 审批状态，使用 ApprovalStatusEnum 枚举值（0-草稿、1-审批中、2-已通过、3-已驳回、4-已撤回） |
| `approval_instance_id` | BIGINT | 关联审批实例ID，关联 hr_approval_instance.id |


---

## 五、权限体系设计
### 5.1 角色定义
| 角色标识 | 角色名称 | 数据范围 | 核心职责 |
| --- | --- | --- | --- |
| `ADMIN` | 系统管理员 | 全平台 | 系统配置、角色管理、数据备份 |
| `HR` | HR 专员 | 全部员工 | 员工管理、薪资核算、考勤管理、审批管理 |
| `MANAGER` | 部门主管 | 本部门及下属 | 本部门员工查看、下属审批 |
| `FINANCE` | 财务专员 | 薪资相关 | 薪资审核、成本报表查看 |
| `EMPLOYEE` | 普通员工 | 仅本人 | 个人信息、请假申请、工资条查看 |


### 5.2 权限标识规范
遵循 `{模块}:{资源}:{操作}` 三段式格式：

| 段落 | 说明 | 示例 |
| --- | --- | --- |
| 模块 | 对应后端模块名 | `system`、`employee`、`salary` |
| 资源 | 对应具体业务实体 | `user`、`employee`、`salary` |
| 操作 | 对应操作类型 | `list`、`create`、`edit`、`delete`、`approve` |


**权限标识示例**：

| 模块 | 权限标识 | 说明 |
| --- | --- | --- |
| auth | `system:user:list` | 查看用户列表 |
| auth | `system:user:create` | 创建用户 |
| auth | `system:user:edit` | 编辑用户 |
| auth | `system:user:delete` | 删除用户 |
| organization | `organization:dept:list` | 查看部门列表 |
| organization | `organization:dept:create` | 创建部门 |
| employee | `employee:list` | 查看员工列表 |
| employee | `employee:create` | 创建员工 |
| personnel | `personnel:entry:create` | 发起入职 |
| personnel | `personnel:entry:approve` | 审批入职 |
| attendance | `attendance:leave:create` | 发起请假 |
| attendance | `attendance:leave:approve` | 审批请假 |
| salary | `salary:accounts:list` | 查看薪资账套 |
| approval | `approval:task:list` | 查看待办列表 |
| approval | `approval:task:approve` | 审批操作 |
| mycenter | `mycenter:profile:view` | 查看个人档案 |
| mycenter | `mycenter:salary:view` | 查看个人薪资 |
| ai | `ai:chat` | AI对话 |
| ai | `ai:knowledge:manage` | 知识库管理 |


### 5.3 数据权限范围
| data_scope 值 | 含义 | 过滤逻辑 | 适用角色 |
| :---: | --- | --- | --- |
| 1 | 仅本人 | `creator_id = 当前用户ID` | EMPLOYEE |
| 2 | 本部门 | `department_id = 当前用户部门ID` | — |
| 3 | 本部门及下属部门 | `department_id IN (子查询本部门及所有下级部门ID)` | MANAGER |
| 4 | 全部 | 不追加过滤条件 | ADMIN、HR |


**说明**：数据范围值为 `sys_role.data_scope` 字段的存储值，通过 MyBatis 拦截器 `DataScopeInterceptor` 在 SQL 执行前动态注入过滤条件。各级范围的具体数值与后端 `DataScopeEnum` 枚举一致。

---

## 六、技术栈选型
| 层级 | 技术 | 版本 | 用途说明 |
| --- | --- | --- | --- |
| 后端框架 | Spring Boot | 3.5.16 | 单体应用框架 |
| Java 版本 | Java | 17 | 开发语言 |
| ORM | MyBatis-Plus | 3.5.12 | 数据持久化 |
| 数据库 | MySQL | 8.0+ | 主数据库 |
| 缓存 | Redis | - | 会话管理、数据缓存、分布式锁 |
| 消息队列 | RabbitMQ | - | 异步通知、削峰填谷 |
| 前端框架 | React + TypeScript | 18 / 5 | 前端开发 |
| 前端脚手架 | UmiJS | - | 前端工程化 |
| 包管理 | pnpm | 11.10.0 | 依赖管理 |


---

## 七、版本记录
| 版本 | 日期 | 说明 |
| --- | --- | --- |
| v1.0 | 2026-07-11 | 初版，从 HRMS 模块划分规范中提取前置约定，作为所有后端开发的总依据 |
| v1.1 | 2026-07-11 | 统一成功码（0→20000）、实体命名（XxxDO→XxxEntity）、错误码体系（重写为 20000~50002 码段），补充 version 字段、数据权限对齐后端系分 |


