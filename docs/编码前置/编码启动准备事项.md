# HRMS 编码启动前准备事项
> **版本**: v1.0  
**日期**: 2026-07-11  
**目标**: 完成以下 5 项准备后，4 人可直接按分工并行编码
>

---

## 事项一：统一核心规范冲突
### 1.1 成功码统一
| 文档 | 当前值 | 决策建议 |
| :--- | :---: | :---: |
| 前置说明.md §3.1 | `code: 0` | **以后端系分为准 → code: 20000** |
| 后端系分 §2.5.3 | `code: 20000` | 保持 |


**理由**：后端系分有完整的 18 行异常码表（20000~50002），前端 request 拦截器已按 `code !== 20000` 实现。若改为 0，整套异常码体系需重新设计。

### 1.2 实体命名统一
| 文档 | 当前值 | 决策建议 |
| :--- | :---: | :---: |
| 前置说明.md §2.1 | `XxxDO`（UserDO） | **以后端系分为准 → XxxEntity** |
| 后端系分 §2.5.2 | `XxxEntity`（UserEntity） | 保持 |


**理由**：后端系分明确写 `*Entity` + 继承 `BaseEntity`，更符合 Spring Boot 实体类命名习惯（与 `@Entity` 注解语义一致）。

### 1.3 数据权限范围值统一
| 文档 | 当前值 | 决策建议 |
| :--- | :---: | :---: |
| 前置说明.md §5.3 | 3=本部门及下属部门, 4=全部 | **以后端系分为准** |
| 后端系分 §4.2 | 3=本部门及下属部门, 4=全部 | 保持 |


**理由**：后端系分有完整的 DataScopeInterceptor 过滤 SQL 示例，四级模型定义最精细。

### 1.4 操作清单（已完成 ✅）
所有操作已于 2026-07-11 执行完毕：

- [x] 将 `前置说明.md` 的 `§3.1` 成功码由 `0` 改为 `20000`，JSON 示例同步修改
- [x] 将 `前置说明.md` 的 `§2.1` 命名规范由 `XxxDO` 改为 `XxxEntity`
- [x] 将 `前置说明.md` 的 `§2.4` 错误码分段重写为后端 `20000~50002` 体系，含完整 18 行异常码表 + 模块业务码参考表
- [x] 将 `前置说明.md` 的 `§3.2` ErrorCode 枚举定义重写（SUCCESS=20000, FORBIDDEN=40300 等）
- [x] 将 `前置说明.md` 的 `§3.3` BaseEntity 增加 `version` 字段
- [x] 将 `前置说明.md` 的 `§5.3` 数据权限范围对齐后端系分（3=本部门及下属部门, 4=全部）

---

## 事项二：抽取可执行建表脚本
### 2.1 来源
后端总体系统分析说明书中各模块的 DDL 语句（§3.1~§3.9），涉及 9 个模块约 30+ 张表。

### 2.2 表清单
| 模块 | 表名 | 后端系分位置 |
| :--- | :--- | :---: |
| M5 权限体系 | `sys_user` | §3.1.4 |
| M5 权限体系 | `sys_role` | §3.1.4 |
| M5 权限体系 | `sys_menu` | §3.1.4 |
| M5 权限体系 | `sys_user_role` | §3.1.4 |
| M5 权限体系 | `sys_role_menu` | §3.1.4 |
| M6 组织架构 | `sys_dept` | §3.2.4 |
| M6 组织架构 | `sys_post` | §3.2.4 |
| M6 组织架构 | `sys_dict_type` | §3.2.4 |
| M6 组织架构 | `sys_dict_data` | §3.2.4 |
| M1 员工档案 | `hr_employee` + 扩展表 | §3.3.4 |
| M2 入转调离 | `hr_entry_application` 等 | §3.4.4 |
| M3 考勤管理 | `hr_attendance_group`, `hr_attendance_record`, `hr_leave_request`, `hr_attendance_correction` | §3.5.4 |
| M4 薪资管理 | `hr_salary_account` 等 | §3.6.4 |
| M7 审批中心 | `hr_approval_instance` 等 | §3.7.4 |
| M8 个人中心 | `hr_leave_record` 等 | §3.8.3 |
| M9 AI 助手 | `hr_ai_conversation` 等 | §3.9.3 |
| 公共 | `sys_file` / `sys_operate_log` / `sys_login_log` | §4.7 |


### 2.3 规范要求
- [ ] 所有表继承公共字段：`id`, `create_by`, `create_time`, `update_by`, `update_time`, `is_deleted`, `version`
- [ ] 字符集：`utf8mb4`，collation `utf8mb4_0900_ai_ci`
- [ ] 存储引擎：`InnoDB`
- [ ] 主键：`BIGINT UNSIGNED AUTO_INCREMENT`
- [ ] 索引命名：`idx_表名_字段名`，唯一索引 `uk_表名_字段名`

### 2.4 输出物
```plain
数据库/
└── init.sql          # 所有 CREATE TABLE + 索引 + 外键
```

---

## 事项三：准备种子数据
### 3.1 必须的种子数据
| 表 | 数据 | 说明 |
| :--- | :--- | :--- |
| `sys_user` | admin / 123456 | 超级管理员（登录用） |
| `sys_role` | ADMIN / HR / MANAGER / FINANCE / EMPLOYEE | 5 个角色 |
| `sys_user_role` | admin → ADMIN 角色 | 关联 |
| `sys_menu` | 目录→菜单→按钮的三级菜单树 | 导航用 |
| `sys_role_menu` | 每个角色可访问的菜单 | 权限控制 |
| `sys_dict_type` | 请假类型、证件类型、学历等 | 下拉框数据源 |
| `sys_dict_data` | 各字典类型的具体选项值 | 下拉框选项 |
| `sys_dept` | 至少一个根部门 | 组织架构基础 |
| `sys_post` | 至少一个默认职位 | 员工档案基础 |


### 3.2 输出物
```plain
数据库/
└── seed.sql          # 所有 INSERT 初始数据
```

- [ ] 管理员默认密码用 BCrypt 加密后的值
- [ ] 菜单树的 `order_num` 排序字段正确
- [ ] 字典数据的父子关系正确

---

## 事项四：搭建空项目骨架
### 4.1 Maven 多模块结构
```plain
hrms/
├── pom.xml                          # 父 POM，统一版本管理
├── hrms-common/
│   └── pom.xml
│   └── src/main/java/com/hrms/common/
│       ├── web/
│       │   ├── Result.java           # 统一返回体
│       │   ├── PageResult.java       # 分页返回体
│       │   └── GlobalExceptionHandler.java  # 全局异常处理器
│       ├── exception/
│       │   ├── ErrorCode.java        # 错误码枚举
│       │   └── GlobalException.java  # 全局业务异常（统一使用，禁止各模块自定异常类）
│       ├── entity/
│       │   └── BaseEntity.java       # 公共字段基类
│       ├── enums/
│       │   ├── BaseEnum.java         # 枚举基类接口
│       │   ├── EmployeeStatusEnum.java
│       │   ├── GenderEnum.java
│       │   ├── LeaveTypeEnum.java
│       │   ├── ApprovalStatusEnum.java
│       │   ├── DataScopeEnum.java
│       │   └── MenuTypeEnum.java
│       ├── security/
│       │   └── SecurityContextHolder.java  # 登录上下文
│       └── config/
│           └── MyMetaObjectHandler.java    # 自动填充
├── hrms-system/                     # 系统底座
│   └── pom.xml
│   └── src/main/java/com/hrms/system/
│       ├── auth/
│       ├── organization/
│       ├── file/
│       └── log/
├── hrms-business/                   # 核心业务
│   └── pom.xml
│   └── src/main/java/com/hrms/business/
│       ├── employee/
│       ├── personnel/
│       ├── attendance/
│       ├── salary/
│       ├── approval/
│       ├── mycenter/
│       └── ai/
└── hrms-server/                     # 启动装配
    └── pom.xml
    └── src/main/java/com/hrms/
    │   └── HrmsApplication.java
    └── src/main/resources/
        ├── application.yml
        ├── application-dev.yml
        └── logback-spring.xml
```

### 4.2 依赖清单（父 POM）
| 依赖 | 版本 |
| :--- | :---: |
| Spring Boot | 3.5.16 |
| Java | 17 |
| MyBatis-Plus | 3.5.12 |
| MySQL Connector | 8.0.33 |
| Redis (Spring Data) | 由 Spring Boot BOM 管理 |
| RabbitMQ (Spring AMQP) | 由 Spring Boot BOM 管理 |
| jjwt | 0.12.6 |
| SpringDoc OpenAPI | 2.8.6 |
| Lombok | 1.18.36 |
| MapStruct | 1.6.3 |
| Hibernate Validator | 由 Spring Boot BOM 管理 |


### 4.3 输出物
- [ ] Git 仓库初始化（`git init` + `.gitignore`）
- [ ] 父 POM 版本管理
- [ ] 4 个 Maven 子模块
- [ ] `common` 模块基础类编写完毕
- [ ] `HrmsApplication.java` 启动类

---

## 事项五：编写公共配置
### 5.1 application.yml 配置项
| 配置 | 说明 |
| :--- | :--- |
| `server.port: 8080` | 启动端口 |
| `spring.datasource` | MySQL 连接（url/username/password/driver-class-name） |
| `spring.redis` | Redis 连接（host/port/database） |
| `spring.rabbitmq` | RabbitMQ 连接（host/port/username/password） |
| `mybatis-plus` | 全局配置（逻辑删除、分页插件、自动填充） |
| `jwt.secret` | JWT 签名密钥（HS256） |
| `jwt.expiration: 7200` | Token 有效期（2 小时） |
| `springdoc.api-docs.enabled: true` | Swagger 开关 |
| `logging.level.com.hrms: DEBUG` | 开发环境日志级别 |


### 5.2 输出物
- [ ] `application.yml`（公共配置 + 默认值）
- [ ] `application-dev.yml`（开发环境覆盖：数据源、Redis、RabbitMQ 连接信息）
- [ ] `application-prod.yml`（生产环境覆盖：更严格的日志、关闭 Swagger）

---

## 附录：分工对照表
| 开发者 | 负责模块 | 准备期间任务 |
| :--- | :--- | :--- |
| **地基搭建者** | common + file + log | 事项一~五全部执行 |
| **成员 A** | auth + organization | 熟悉 auth/org DDL + API，等待事项二/四完成后开写 |
| **成员 B** | employee + personnel | 熟悉 employee/personnel DDL + API |
| **成员 C** | attendance + salary | 熟悉 attendance/salary DDL + API |
| **成员 D** | approval + mycenter + ai | 熟悉 approval/mycenter/ai DDL + API |


### 启动检查清单（所有人完成以下才算准备就绪）
- [ ] 前置说明与系分文档的规范冲突已统一
- [ ] `init.sql` 合并完成，可在 MySQL 中执行成功
- [ ] `seed.sql` 写入初始数据，项目启动后可登录
- [ ] Git 仓库已初始化，4 个 Maven 模块结构就绪
- [ ] `common` 基础类已编写（R / ErrorCode / BaseEntity / 枚举 / SecurityContextHolder / MetaObjectHandler）
- [ ] `application.yml` 配置齐全，`HrmsApplication` 可正常启动
- [ ] 4 人本地环境一致：Java 17 + MySQL 8.0 + Redis 7.x + Maven 3.9+

