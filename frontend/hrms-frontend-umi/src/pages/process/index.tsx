/**
 * 流程页面
 * 负责人：成员 B
 */

import React from 'react';
import { PageContainer } from '@ant-design/pro-components';

/**
 * 入转调离流程首页组件。
 * 当前用于承载流程模块入口占位内容，后续可扩展为统一导航页。
 */
const ProcessPage: React.FC = () => {
  return (
    <PageContainer>
      <div>入转调离流程页面（待开发）</div>
    </PageContainer>
  );
};

export default ProcessPage;
