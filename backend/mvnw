#!/usr/bin/env bash
# Maven Wrapper script
# This is a simplified wrapper that calls mvn if available, otherwise shows an error

if command -v mvn >/dev/null 2>&1; then
    exec mvn "$@"
else
    echo "Maven is not installed. Please install Maven 3.9+ and add it to your PATH."
    exit 1
fi
