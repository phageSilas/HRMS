<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hrms.system.auth.mapper.RoleMenuMapper">

    <!-- 根据角色 ID 列表查询菜单 ID 列表 -->
    <select id="selectMenuIdsByRoleIds" resultType="java.lang.Long">
        SELECT menu_id FROM sys_role_menu
        WHERE role_id IN
        <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
            #{roleId}
        </foreach>
          AND is_deleted = 0
    </select>

    <!-- 查询角色的所有菜单关联（包括已删除的） -->
    <select id="selectAllByRoleId" resultType="com.hrms.system.auth.entity.RoleMenuEntity">
        SELECT id, role_id, menu_id, is_deleted, version
        FROM sys_role_menu
        WHERE role_id = #{roleId}
    </select>

</mapper>
