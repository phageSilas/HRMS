<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hrms.system.auth.mapper.RoleMapper">

    <!-- 按角色 ID 查询菜单列表 -->
    <select id="selectMenusByRoleId" resultType="com.hrms.system.auth.entity.MenuDO">
        SELECT m.*
        FROM sys_menu m
        INNER JOIN sys_role_menu rm ON m.id = rm.menu_id
        WHERE rm.role_id = #{roleId}
          AND m.is_deleted = 0
          AND rm.is_deleted = 0
        ORDER BY m.sort_no
    </select>

</mapper>