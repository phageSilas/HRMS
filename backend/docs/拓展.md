# 后端拓展文档

> 本文档归档了非当前开发阶段必需的文档内容，包括运维配置、安全加固、部署相关等。

---

## 一、运维配置文档

### 1.1 部署相关

#### 部署检查清单

**环境变量配置（必须）**

##### JWT 配置
- [x] ✅ 设置 `JWT_SECRET` 环境变量（已生成：`6mX398roeq2NMrbwVkKZ3K0OOO1J9zk+RK3ZrDnFLiA=`）
- [ ] 确认 JWT 密钥长度至少 32 字节
- [ ] 确保 JWT 密钥未硬编码在代码中

##### 数据库配置
- [ ] 设置 `DB_HOST`（数据库服务器地址）
- [ ] 设置 `DB_PORT`（默认 3306）
- [ ] 设置 `DB_NAME`（数据库名称）
- [ ] 设置 `DB_USERNAME`（数据库用户名）
- [ ] 设置 `DB_PASSWORD`（数据库密码）

##### 文件上传配置
- [x] ✅ 设置 `FILE_UPLOAD_PATH`（默认：`D:/hrms/files`）

#### 安全配置（必须）

##### 默认账号密码
- [ ] 修改默认管理员密码（默认：admin/admin123）
- [ ] 建议新密码：`Admin@2026!HRMS` 或更强的密码

#### 数据库准备（必须）

##### 数据库创建
```sql
CREATE DATABASE hrms DEFAULT CHARACTER SET utf8mb4;
```

##### 表结构初始化
执行表结构脚本（见 `docs/sql/schema/`）

---

### 1.2 HTTPS配置

#### 获取SSL证书
- Let's Encrypt（免费）
- 商业证书

#### 配置Nginx反向代理
```nginx
server {
    listen 443 ssl;
    server_name your-domain.com;

    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;

    location / {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

#### 强制HTTPS重定向
```nginx
server {
    listen 80;
    server_name your-domain.com;
    return 301 https://$server_name$request_uri;
}
```

---

### 1.3 防火墙配置

#### Linux防火墙（firewalld）
```bash
# 开放必要端口
firewall-cmd --permanent --add-port=22/tcp
firewall-cmd --permanent --add-port=80/tcp
firewall-cmd --permanent --add-port=443/tcp
firewall-cmd --reload
```

#### Windows防火墙
```powershell
# 开放端口
New-NetFirewallRule -DisplayName "HRMS HTTP" -Direction Inbound -LocalPort 80 -Protocol TCP -Action Allow
New-NetFirewallRule -DisplayName "HRMS HTTPS" -Direction Inbound -LocalPort 443 -Protocol TCP -Action Allow
```

---

## 二、安全加固文档

### 2.1 已修复的安全问题

#### ✅ JWT密钥硬编码（CRITICAL）

**问题**: JWT密钥直接硬编码在 `application.yaml` 中

**修复**: 使用环境变量
```yaml
jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION:604800000}
```

---

#### ✅ 文件预览路径遍历漏洞（HIGH）

**问题**: `FileController.preview` 未验证文件路径

**修复**: 添加路径验证
```java
// 防止路径遍历攻击
if (fileVO.getFilePath().contains("..")) {
    return ResponseEntity.status(HttpStatus.FORBIDDEN).build();
}
```

---

#### ✅ 密码验证逻辑缺陷（MEDIUM）

**问题**: 部分接口未验证密码

**修复**: 添加密码确认验证

---

### 2.2 安全配置建议

#### 数据库安全
- [ ] 删除测试数据库
- [ ] 禁止root远程登录
- [ ] 定期备份数据库

#### 应用安全
- [ ] 添加登录速率限制
- [ ] 配置CORS白名单
- [ ] 启用请求日志记录

---

## 三、部署配置总结

### 3.1 已完成配置

#### 环境变量
- ✅ JWT_SECRET 已生成
- ✅ 数据库密码配置
- ✅ 文件上传路径配置

#### 数据库
- ✅ 数据库创建完成
- ✅ 表结构初始化完成
- ✅ 初始数据导入完成

#### 应用配置
- ✅ Spring Boot配置完成
- ✅ MyBatis-Plus配置完成
- ✅ 跨域配置完成

---

### 3.2 待完成配置

#### 生产环境
- [ ] HTTPS证书配置
- [ ] 防火墙规则配置
- [ ] 监控告警配置

#### 性能优化
- [ ] 数据库索引优化
- [ ] JVM参数调优
- [ ] 连接池配置

---

## 四、测试报告归档

### 测试报告-20260710

**测试时间**: 2026-07-10 09:43:34

**测试结果**:
- ✅ 编译测试通过
- ✅ 应用启动测试通过
- ✅ 数据库连接测试通过
- ✅ 部门接口测试通过
- ❌ 登录接口测试失败（已修复）

**发现问题**:
1. 数据库表结构不匹配（已修复）
2. BCrypt密码hash格式错误（修复中）
3. Actuator端点未配置（待修复）

**修复方案**: 见 `密码修复说明.md`

---

## 五、密码修复说明

### 问题描述
登录失败，日志显示: `Encoded password does not look like BCrypt`

### 根本原因
数据库中的密码hash不是有效的BCrypt格式

### BCrypt Hash要求
- 格式: `$2a$` + 2位cost + `$` + 53位base64编码
- 总长度: 60个字符
- 示例: `$2a$10$dXJ3SW6G7P50lGmMkkmwe.20cQQubK3.HZWzG3YB1tlRy.fqvM/BG`

### 解决方案
在AuthController中添加临时接口生成正确的BCrypt hash:
```java
@GetMapping("/gen-password")
public Result<String> generatePassword(@RequestParam String password) {
    String hash = PasswordUtils.encode(password);
    return Result.success(hash);
}
```

访问: `GET /auth/gen-password?password=admin123`

---

## 六、文档归档说明

### 归档文档列表

| 原文档 | 归档原因 |
|--------|---------|
| 安全问题修复报告.md | 开发阶段暂不需要,生产部署时参考 |
| HTTPS配置指南.md | 开发环境不需要,生产部署时参考 |
| 防火墙配置指南.md | 开发环境不需要,生产部署时参考 |
| 部署检查清单.md | 开发阶段暂不需要,生产部署时参考 |
| 部署配置完成总结.md | 开发阶段暂不需要,生产部署时参考 |
| 测试报告-20260710.md | 历史测试报告,归档备查 |
| 密码修复说明.md | 临时修复文档,问题解决后归档 |

### 当前保留文档

| 文档 | 保留原因 |
|------|---------|
| README.md | 文档索引,必须保留 |
| 协同开发文档.md | 开发流程说明,开发阶段必需 |
| API接口文档.md | 接口说明,开发阶段必需 |
| 开发问题修复记录.md | 问题追踪,开发阶段必需 |
| 后端系统底座使用指南.md | 功能使用说明,开发阶段必需 |

---

**归档时间**: 2026-07-10
**归档原因**: 优先让后端项目运行起来,运维和部署文档暂时归档