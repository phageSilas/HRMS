# Dev Profile
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hrms?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=GMT%2B8&allowPublicKeyRetrieval=true
    username: root
    password: 230814
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.zaxxer.hikari.HikariDataSource
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      auto-commit: true
      idle-timeout: 30000
      max-lifetime: 600000
      connection-timeout: 30000
      pool-name: HikariPool
  data:
    redis:
      host: localhost
      port: 6379
      password: ""
      database: 0
      timeout: 3000ms
      lettuce:
        pool:
          max-active: 20
          max-idle: 10
          min-idle: 5

# JWT
hrms:
  jwt:
    secret: 6mX398roeq2NMrbwVkKZ3K0OOO1J9zk+RK3ZrDnFLiA=
    expiration: 604800000
    header: Authorization
    token-prefix: Bearer

# Logging
logging:
  level:
    com.hrms: DEBUG
    com.baomidou: DEBUG
